<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/start_blue</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="toolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">@color/start_blue</item>
        <item name="colorPrimary">@color/start_blue</item>
        <item name="android:statusBarColor">@color/start_blue</item>
        <item name="colorControlNormal">@color/colorTitleWhite</item>
        <item name="android:icon">@drawable/toolbar_icon</item>

    </style>

</resources>
